import { PizzasSection } from "./components/PizzasSection";
import { DefaultLayout } from "../../layouts";

export const Home = () => {
  return (
    <DefaultLayout>
      <PizzasSection />
    </DefaultLayout>
  );
};
